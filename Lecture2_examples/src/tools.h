#ifndef TOOLS_H_
#define TOOLS_H_

#include <iostream>
#include <cmath>
using namespace std;

void rude_responder();
void print_line(int);
void print_stars(int, int);
int sum_in_range(int, int);
bool isprime(int);
void print_prime_in_range(int, int);
int gcd(int, int);
double find_root(double);

#endif /* TOOLS_H_ */
